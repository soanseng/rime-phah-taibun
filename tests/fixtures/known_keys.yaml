# Known-keys smoke gate (PLAN 9-2G)
# Each line: <rime key>: <minimum rows>. A row hits a key on exact match
# or first-syllable prefix. Thresholds calibrated at ~70% of the dictionary
# at the time of recalibration; catches large-scale regressions such as
# converter breakage or a dropped source database.
#
# 2026-09 v0.8.0 recalibration: identity-keyed dedup, top-weight lighttone
# prefix assembly, and non-CJK assembly filtering intentionally reduced
# per-key rows (si7 356→152). Lost identities are: duplicate rows,
# romanization-text assemblies ("?--了", "sī--了"), and rare-prefix
# lighttone assemblies (鼓--了 — the character 鼓/si7 stays selectable
# as a single-syllable candidate; only its lighttone compound form is
# pruned). This is an accepted trade: same-reading ties no longer hijack
# sentence composition (佐--的 vs 做--的).
#
# Recalibrate after intentional dictionary changes:
#   uv run python scripts/validate_dict.py schema/phah_taibun.dict.yaml \
#       --known-keys tests/fixtures/known_keys.yaml

tsiah8: 400
png7: 80
tai5: 240
uan5: 130
gua2: 60
beh4: 65
sui2: 70
kha1: 380
tsit8: 400
e7: 260
si7: 106
a2: 18
i1: 100
u7: 440
tsiah8 png7: 8
tai5 uan5: 35
tsit8 e7: 10
